*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
scripts/soak/logs/
# Release signing — never commit the upload keystore or its credentials (see keystore.properties.example).
keystore.properties
*.jks
*.keystore

# Trailer pipeline (scripts/record-trailer.sh + scripts/build-trailer.sh): raw captures and ffmpeg
# stage intermediates are large + regenerable; the finished masters in trailer/out/ are kept. A
# user-supplied music bed (assets/trailer/*) is not committed (check its license).
/trailer/
/assets/trailer/

app/release/*

# Kover coverage Stop-hook (.claude/hooks/gradle-kover-stop.sh): per-clone high-water baseline,
# re-banked on every commit. Machine-local state, not shared — regenerated on the next Stop.
.claude/hooks/.kover-baseline